'use strict';

var React = require('react');

var allBallots = this.props.ballots;
var ballots = [];

for (var key in allBallots){
  
}

var Election = React.createClass({
  render: function() {
    return (
      <div>
        <Header>
        <Election
          ballots={this.props.ballots}
        />
        <SubmitButton>
        <Footer>
    );
  }

});

module.exports = VotingBooth;