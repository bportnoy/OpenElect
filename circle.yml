dependencies:
  pre:
    - bower install

deployment:
  # production:
    # branch: production
    # commands:
    #   - ./deploy_prod.sh

  staging:
    branch: master
    owner: SkillfulCactus
    commands:
      - ./deploy-staging.sh