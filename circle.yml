dependencies:
  pre:
    - bower install

test:
  override:
    - grunt test

deployment:
  # production:
    # branch: production
    # commands:
    #   - ./deploy_prod.sh

  staging:
    branch: master
    commands:
      - sudo chmod +x deploy-staging.sh
      - ./deploy-staging.sh