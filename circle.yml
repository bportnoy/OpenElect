dependencies:
  pre:
    - bower install

deployment:
  # production:
    # branch: production
    # commands:
    #   - ./deploy_prod.sh

machine:
  environment:
    DBURL: postgres://ubuntu:@127.0.0.1:5432/circle_test

  staging:
    branch: master
    commands:
      - ./deploy-staging.sh